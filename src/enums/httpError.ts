export enum MongooseErrors {
  "CAST_ERROR" = "Error to cast ObjectId for value id",
}

export enum HttpErrorsEnum {
  "PARAMS_ERROR_TYPES" = "The param has not valid types",
}

export enum HttpErrorsCode {
  "STATUS_404" = "Error to find out the resource or the URL is not correct",
}
